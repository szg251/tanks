jobs:
  include:
    - stage: "Test"
      language: elixir
      elixir: "1.8.0"
      otp_release: "21.2"

    - stage: "Test"
      language: elm
      elm: "0.19.0"
      before_script: cd assets

    - stage: "Deploy"
      deploy:
        provider: script
        skip_cleanup: true
        script: bash scripts/deploy.sh
        on:
          branch: master
